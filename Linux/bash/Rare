ДИСКОВОЕ МЕСТО

df -h - Список всех файловых систем по именам устройств, сообщает их размер, занятое и свободное пространство и точки монтирования.

lshw - Утилита предоставляет детальную информацию о конфигурации оборудования компьютера.

file - определяет тип файла

less - выводит содержимое файла

ln - создает жесткие символические ссылки

alias - создает псевдоним для команды

unalias - убрать псевдоним

man - документация команды

which - какая команда будет выполнена

wc -l - количество слов в файле (строкб байт)

head - вывести первые 10 строк

tail - вывести последние 10 строк

echo - выводит строку текста

history - выводит содержимое команд

script - записать команды в файл

id - информация о пользователе

chown - изменение владельца и группы файла

top - выводит задачи

bg - переодит задачу в фоновый режим

fg - переводит задачу в режим а передний план

kill - посылает сигнал процессу

pstree - выводит список процессов в виде древовидной структуры

vmstat - мгновенный снимок процессов

xload - в графическом интерфейсе

tload - в консоле

export - экспорт окружения для программ

set - выводит переменные командной оболочки и окружения

mount - монтирует файловую систему

unmount - размонтирует файловую систему

fdisk - инструмент для работы с таблицей разделов

mkfs - создает файловые системыы

md5sum - вычисляет контрольную сумму md5

ping - посылает icmp запросы узлам

traceroute - выводит трассировку маршрута

netstat - выводит список сетевых соединений

lftp - передача файлов через интернет

wget - загрузчик файлов из сети

sftp - программа безопасного передачи файлов на удаленный сервер

locate - выполняет поиск файлов по именам

find - поиск файлов в иерархии каталогов

gzip - сжимает и распаковывает файлы

bzip - программа поблочного сжатия

tar - архивирование

zip - упаковывает и сжимает

rsync - синхронизирует файлы с удаленной системой

egrep или grep -e - расширеная версия grep (все метасимволы)

sort - сортирует строки из текстовых файлов

uniq - сообщает о повторяющихся строках или удаляет их

cut - удаляет фрагменты из каждой строки

paste - слияние строк из файлов

diff - построчное сравнение

patch - применят diff файл к оригиналу

tr - перекодирует или удаляет символы

sed - потоковый редактор  для фильтрации и преобразования текста

ssh-keygen - генерация ключей




https://habr.com/ru/post/481398/

