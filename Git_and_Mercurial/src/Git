git config <параметр> <значение> - Установить параметры git
    Пример:
    git config --global user.name <USER_NAME>

    --list - Просмотр всех параметров git
    --global - Параметры для пользователя в системе
    --system - Общие параметры системы
    --local - По умолчанию, параметры в директории .git

git push <сокращение удаленного репозитория> <имя ветки>- Запушить в репозиторий с именем и паролем:
    -u -
    git push https://username:password@github.com/username/repository.git

git init - создать локальный репозиторий

git add - добавить файл под версионный контроль

git rm <имя файла> - Удадяет файл
    --cached - Оставляет файл не под версионным контролем

git status - Показывает файлы под версионным контролем и изменения

git commit -am "<комментарий>" - Коммит
    --amend - изменяет последний комит (создает новый и меняет HEAD)

git reset <hash коммита> - откат к конкретному комиту (по умолчанию к HEAD)
    --soft -
    --mixed -
    --hard -

git checkout <название ветки или hash коммита> - Переход к выбранному коммиту или ветке
    -b - Создать новую ветку и перейти в нее
    -f -

git log - история коммитов
    --author="<имя автора>" -
    --pretty=format:"%H %S %AD %AN" -
        %H хэш
        %S комментарий
        %AD дата
        %AN автор
    --date=short -

git reflog -логи изменения ссылок HEAD

git revert HEAD --no-edit - Отменяет последний коммит

git remote add <сокращение> <url> - Добавляет удаленный репозиторий
    -v - Посмотреть, какому URL соответствует сокращённое имя в Git

git show <название ветки или hash коммита> - показать коммит

git branch -d <название ветки> - Удаление ветки

git tag <имя тэга> <хэш коммита> - Создать тэг, который указывает на коммит

git diff <hash коммита> - показать изменения текущей версии с коммитом

git rebase <имя ветки1> <имя ветки2> - все коммиты <ветки2> переносит на последний коммит <ветки1>

git rebase -i HEAD~4   объеиняем 4 коммита
затем в редакторе вместо pick пишем s (squash) для склейки,  (i  -> insert) далее :wq выходим

git push origin --delete <имя ветки> удалить ветку в удалённом репозитории
